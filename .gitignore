.worktrees/
node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Skills Prisma vendorisées par un outil externe (doublons + symlinks absolus).
# Reproductibles depuis apps/api/skills-lock.json, qui lui est versionné.
apps/api/.agents/
apps/api/.windsurf/
apps/api/.claude/skills/

# Rapports de couverture générés par les suites de tests
coverage/

# Sauvegardes de base de donnees locales
.db-backups/
