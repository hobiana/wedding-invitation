# Production deployment (Vercel): VITE_API_URL must be set as an environment
# variable in the Vercel project settings (pointing at the deployed API URL) —
# this .env.example is for local development only and is not read in production.
VITE_API_URL=http://localhost:3000
